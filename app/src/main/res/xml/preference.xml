<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto">

    <SwitchPreferenceCompat
        app:icon="@drawable/ic_save"
        app:key="@string/pref_key_save_data"
        app:title="Save writing data" />

    <SwitchPreferenceCompat
        app:key="@string/pref_key_auto_evaluate_input"
        app:title="Enable auto evaluate input" />

    <SwitchPreferenceCompat
        app:key="@string/pref_key_auto_clear_canvas"
        app:title="Enable auto clear canvas" />

    <EditTextPreference
        app:key="@string/pref_key_hint_text_type_alpha"
        app:title="あア漢 hint alpha value" />
</PreferenceScreen>
